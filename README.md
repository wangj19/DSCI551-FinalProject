# DSCI551-FInalProject
This is the final project for course DSCI551. The current project topic is online book store.

Below is the command line to install required python library:
pip install Flask
pip install pymongo
